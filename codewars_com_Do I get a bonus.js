function bonusTime(salary, bonus) {
if(bonus == true){ 
    return salary = ('£'+salary * 10);
}else{ if(bonus == false){
    return salary = ('£'+salary * 1);
    }
}
 return bonusTime(10000, true);
 return bonusTime(25000, true);
 return bonusTime(10000, false);
 return bonusTime(60000, false);
 return bonusTime(2, true);
 return bonusTime(78, false);
 return bonusTime(67890, true);
 
}

function oddOrEven(array) {
if (array.length == 0){ return 'even'}else{   //  если массив пустой то четное значение 
var arr = array.reduce(function(a, b){ return (a + b)});
return arr=arr%2 ? 'odd' : 'even';
};
}
